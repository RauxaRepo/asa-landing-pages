import {gsap, TweenMax, TimelineMax, Power} from 'gsap';

export default function () {
    let titleCard = document.querySelector('.main-page-card-title'),
        tl = gsap.timeline({repeat: 0, repeatDelay: 0});
        //gsap.set(titleCard, {opacity: 0});
    //console.log('RUNNING')
}

